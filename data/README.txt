This folder holds the database, the ledger snapshots and the backups.

All of it is gitignored except this file and .gitkeep, so a fresh clone arrives
empty and the database is created on the first run. Nothing here is shared, and
nothing here is needed to read the repository — the findings are in README.md.
